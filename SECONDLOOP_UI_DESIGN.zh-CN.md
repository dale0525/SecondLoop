# SecondLoop Desktop 路由设计评审

## 1. 评审状态

- 状态：通过，可以进入实现
- 评审日期：2026-07-15
- 产品范围：SecondLoop macOS Desktop
- 目标视口：`1440 × 900`、`1280 × 800`、`390 × 844`
- 设计方向：安静的编辑式执行台

这份设计冻结 M2 与 M3 的路由结构、视口行为、组件状态和视觉验收标准。实现必须以这里的路由方案为准；如果真实数据或平台限制要求偏离，必须在视觉验收记录中说明。

## 2. 视觉方向

SecondLoop 不是聊天机器人外壳，而是一张每天都会回来的私人工作台。界面采用深墨绿色背景、温和的灰白正文和克制的鼠尾草绿强调色。红色只用于不可逆操作、失败和结果不确定，不用于普通提醒。

视觉记忆点是一条持续存在的“今日线索”：桌面端由左侧窄导航和主内容区顶部的日期、来源状态组成；窄窗口由底部导航延续。信息密度接近编辑部工作台，卡片边界轻，留白明确，不使用大面积渐变、玻璃拟态或装饰性统计图。

排版分为三层：页面标题 28–32px，分区标题 15–17px，正文 14–15px。数字、状态和来源使用更紧凑的 12–13px。交互控件最小触控区域为 `44 × 44`，正文行长在桌面端不超过 760px。

## 3. 路由结构

正式产品只暴露以下路由：

```text
#today        今天
#chat         对话
#actions      行动中心
#memory       记忆
#connections  连接
#settings     设置
```

Desktop 与 Compact Desktop 使用 220px 左侧导航。品牌位于顶部，主路由位于中段，连接状态和设置位于底部。Chat 的会话列表继续使用覆盖式抽屉，避免导航、会话和消息形成三列拥挤布局。

Narrow Window 使用固定底部导航，直接显示“今天、对话、行动、记忆、更多”。“更多”打开底部 Sheet，承载“连接、设置”。底部导航与输入框不能重叠；Chat 运行时的 Stop 控件不能引发布局跳动。

Developer Tools 与 Owner Skills 不出现在正式产品导航中。直接输入对应 hash 时，Host 策略必须关闭入口并回到设置或今天。

## 4. 路由设计

### 4.1 今天 `#today`

Desktop：顶部显示日期、简短问候和当前来源覆盖；下方采用不对称两列。左列约占 62%，依次展示“今日重点、需要回复”；右列展示“等待批准、已确认承诺”。每一项必须包含状态、来源和时间，不展示没有权威依据的推断数字。

Narrow：改为单列，顺序为今日重点、等待批准、需要回复、已确认承诺。底部导航固定，内容保留至少 88px 安全区。

状态：

- 加载中：四个稳定高度的文本骨架，不闪现 fake 数据。
- 空数据：显示“今天暂时没有已确认的紧急事项”。
- 部分能力不可用：在页首显示来源缺口，并只渲染可用来源。
- 可恢复错误：保留已经加载的区块，错误区块内提供重试。
- 不可恢复错误：不展示旧数据，说明本地服务不可用。
- 运行中：正在生成简报时显示来源收集进度，不把草稿当成权威结果。
- 等待批准、成功、失败、结果不确定：复用 Action 状态颜色和文案。

### 4.2 对话 `#chat`

Desktop：保留单列阅读区，最大宽度 820px；左上打开会话抽屉，右上进入设置。消息、reasoning、tool activity、审批和终态按时间顺序呈现。输入区固定在阅读列底部，而不是整个窗口底部。

Narrow：顶部仅保留会话、产品名和设置；输入区固定在底部导航上方。Stop 与 Send 使用同一 `44 × 44` 槽位，切换时输入框纵向位置不变。

状态：首次恢复显示历史加载状态，不闪现 starter；流式文本、reasoning、工具、重连、停止、失败和 interrupted 都保留已持久化内容。模型未配置时，输入区上方显示前往 Connections 的明确动作。

### 4.3 行动中心 `#actions`

Desktop：左侧为状态筛选和行动列表，右侧为权威预览。预览包含动作类型、目标、收件人或资源、风险摘要、不可变参数、幂等状态和审计时间。批准与拒绝位于预览底部，保持清晰的主次关系。

Narrow：列表和详情分成两层导航；打开详情后提供返回。批准按钮固定在安全区上方，不能遮挡预览末尾。

状态必须完整覆盖 `pending approval`、`executing`、`succeeded`、`failed`、`cancelled` 和 `uncertain`。`uncertain` 只提供对账，不提供更换幂等键的快速重试。重复点击批准不能产生第二次执行。

### 4.4 记忆 `#memory`

Desktop：顶部搜索与 proposal/committed/tombstoned 筛选；左侧结果，右侧详情。详情展示来源、证据摘要、置信度、敏感度、保留策略、创建和更新时间。导出是次要动作，遗忘是危险动作。

Narrow：结果和详情分层。遗忘确认使用阻断 Dialog，明确正文会被清除、审计 tombstone 会保留。

状态覆盖加载、空结果、无搜索结果、proposal、committed、tombstoned、版本冲突、导出成功/失败、遗忘确认/成功/失败。任何错误都不能回显被遗忘正文。

### 4.5 连接 `#connections`

Desktop：采用两列状态卡。模型卡显示 endpoint 类型、地址、模型名、密钥是否已安全保存和连接诊断；Mail 卡显示账号、provider、连接状态、scope 和最近诊断。后续 Calendar、Contacts 使用相同卡片契约。

Narrow：单列。秘密字段只显示“已配置/未配置”，输入框离开页面后不保留明文。测试连接期间按钮保持原宽度，避免抖动。

状态覆盖未配置、配置中、测试中、已连接、需要重新认证、离线、权限不足和失败。产品没有对应 Connector 时显示能力缺口，不显示无效开关。

### 4.6 设置 `#settings`

Desktop：设置内容按“外观、语言、隐私与数据、通知、关于”分区，最大宽度 760px。模型和 Mail 连接不重复放在设置中，只提供到 Connections 的链接。开发入口只在明确开发策略下出现。

Narrow：单列分组，行高至少 52px。危险数据操作与普通外观选项分区，不能相邻放置。

状态覆盖加载、保存中、保存成功、保存失败、安全存储不可用、数据保护状态和重启要求。任何保存失败都保留用户仍可核对的非秘密输入。

## 5. 共享组件状态

- 来源标签：显示 provider、资源身份和时间；模型推断使用不同样式，不能伪装成 provider 来源。
- 状态徽标：建议为灰、草稿为蓝灰、等待批准为琥珀、执行中为蓝、成功为绿、失败为红、uncertain 为红色斜纹或明确文字。
- 空状态：一句事实说明和一个下一步，不使用虚构示例填满页面。
- 错误：优先局部恢复；只有 sidecar、App bootstrap 或数据库不可用时使用全页错误。
- Dialog 与 Sheet：使用 Radix primitives，支持 Escape、焦点陷阱、返回焦点和可见焦点环。
- Loading：尺寸稳定，不以无限 spinner 代替状态说明。

## 6. 视口验收标准

### 1440 × 900

- 左侧导航宽度固定，主内容居中但不形成过宽空洞。
- Today 两列层级清楚；Action、Memory 的列表与详情同时可用。
- Chat 阅读列不超过 820px。
- 页面无横向滚动，主要动作无需滚动即可识别。

### 1280 × 800

- 导航与内容仍保持双栏，卡片间距收紧但不压缩触控目标。
- 长标题、邮箱地址、来源和错误文案正确截断或换行。
- Dialog 不超出视口，底部动作始终可达。

### 390 × 844

- 使用底部导航，不显示桌面侧栏。
- 所有主要交互区域至少 `44 × 44`。
- 页面、Sheet、Dialog 和代码/预览内容无横向溢出。
- 固定输入区、批准区和底部导航互不遮挡。
- 中英文标签不截断到无法理解。

## 7. 评审结论

| 路由 | 结构 | Desktop | Compact | Narrow | 状态集合 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| Today | 两列/单列 | 通过 | 通过 | 通过 | 完整 | 可实现 |
| Chat | 阅读列+抽屉 | 通过 | 通过 | 通过 | 完整 | 可实现 |
| Action Center | 列表+预览 | 通过 | 通过 | 通过 | 完整 | 可实现 |
| Memory | 搜索+详情 | 通过 | 通过 | 通过 | 完整 | 可实现 |
| Connections | 状态卡 | 通过 | 通过 | 通过 | 完整 | 可实现 |
| Settings | 分组设置 | 通过 | 通过 | 通过 | 完整 | 可实现 |

实现完成后必须在三个目标视口分别截图，逐项检查布局、间距、字号层级、颜色、状态、响应式行为和文本适配。截图基线通过前，M2 不视为完成。

## 8. 最终实现验收

- 验收状态：通过
- 验收产物：`dist/macos/secondloop/SecondLoop-darwin-arm64/SecondLoop.app`
- 截图目录：`output/playwright/secondloop-final/`
- 截图数量：19 张

六个路由在 `1440 × 900`、`1280 × 800` 和 `390 × 844` 均通过；窄窗口 More Sheet 也通过。所有截图的像素尺寸都经过核对，所有路由的 `document.scrollWidth` 与 `clientWidth` 相等，console warning、error 和未捕获异常均为 0。

关键结果：

- Today 在桌面保持非对称双列，在窄窗口按“今日重点、等待批准、需要回复、已确认承诺”排列。
- Chat 桌面阅读列不超过 820px；窄窗口 Composer 与底部导航没有重叠。
- Action 和 Memory 的桌面列表/详情结构清晰，窄窗口正确收敛为分层或单列空态。
- Connections 和 Settings 的长内容只产生纵向滚动，不产生横向溢出，也不被底部导航遮挡。
- 390px 下主要按钮、底栏项、Composer 和 Sheet 控件的有效触控区域均至少为 `44 × 44`。
- More Sheet 打开后焦点落在关闭按钮；关闭按钮为 `44 × 44`，连接和设置两行均高于 44px。

可接受偏差：

- 隔离 QA 没有真实 Action 或 Memory 数据，因此这两路由验收空态和详情占位；真实详情契约由自动化测试覆盖。
- Chat 采用模型未配置引导态，Connections 采用 Mail 未配置态；两者都是本文冻结的合法产品状态。
- Connections 和 Settings 在 390px 下需要纵向滚动，符合单列设计，不视为布局偏差。
