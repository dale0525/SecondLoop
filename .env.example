# Copy this file to `.env.local` and fill in values.
# `.env.local` is gitignored.
#
# You can leave everything empty if you're only running unit tests or using BYOK.

# Firebase: Project settings → General → Web API Key
SECONDLOOP_FIREBASE_WEB_API_KEY=

# SecondLoopServer AI Gateway base URL (Ask AI + `/v1/embeddings`), e.g. https://<worker>.<subdomain>.workers.dev
# Keep both staging/prod URLs here and switch via SECONDLOOP_CLOUD_ENV.
# NOTE: Do not set `SECONDLOOP_CLOUD_GATEWAY_BASE_URL` directly in `.env.local` (scripts derive it).
SECONDLOOP_CLOUD_ENV=staging
SECONDLOOP_CLOUD_GATEWAY_BASE_URL_STAGING=
SECONDLOOP_CLOUD_GATEWAY_BASE_URL_PROD=

# SecondLoopServer Managed Vault base URL, e.g. https://<worker>.<subdomain>.workers.dev
# Keep both staging/prod URLs here and switch via SECONDLOOP_CLOUD_ENV.
# NOTE: Do not set `SECONDLOOP_MANAGED_VAULT_BASE_URL` directly in `.env.local` (scripts derive it).
SECONDLOOP_MANAGED_VAULT_BASE_URL_STAGING=
SECONDLOOP_MANAGED_VAULT_BASE_URL_PROD=

SECONDLOOP_DESKTOP_RUNTIME_TAG=

# Release automation (maintainers, optional for local dev)
# Used by: pixi run release
RELEASE_LLM_API_KEY=
RELEASE_LLM_MODEL=

# Optional, defaults to https://api.openai.com/v1
RELEASE_LLM_BASE_URL=https://api.openai.com/v1

# Optional full LLM endpoint override.
# Example: https://your-gateway.example/v1/chat/completions
RELEASE_LLM_ENDPOINT=

# Optional auth header override for custom gateways.
# Default behavior tries Authorization/api-key/x-api-key automatically.
RELEASE_LLM_AUTH_HEADER=
RELEASE_LLM_AUTH_SCHEME=Bearer

# Optional TLS CA bundle path for custom RELEASE_LLM_BASE_URL endpoints.
# Use when Python reports CERTIFICATE_VERIFY_FAILED.
RELEASE_LLM_CA_BUNDLE=

# Optional, local troubleshooting only. Set to 1 to skip TLS cert verification in release_ai.py.
# pixi run release --dry-run sets this automatically. Do NOT use for real release publishing.
RELEASE_LLM_INSECURE_SKIP_VERIFY=0

# Optional, local troubleshooting only. Set to 1 to skip TLS cert verification during
# runtime payload validation in release-preflight. Do NOT use for real release publishing.
RELEASE_PREFLIGHT_INSECURE_SKIP_VERIFY=0

# Optional, defaults to zh-CN,en-US
RELEASE_NOTES_LOCALES=zh-CN,en-US
